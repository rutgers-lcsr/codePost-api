// C++ Execution Template
// Note: C++ must be compiled. This template helps if the user submits snippets or assumes implicit includes.
// But mostly it serves to wrap "main" if missing or add headers.

#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <map>
#include <set>
#include <cmath>

// Common headers pre-included

// USER CODE BEGINS

// FILLER_CODE

// If user defines main, this works. 
// If user defines code without main but expected to run? Not standard C++.
// We assume user provides compilable unit or at least providing main.
